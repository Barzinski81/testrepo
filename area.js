function calculateRectangleArea(height, width) {const area = height * width; return area}
const height = 5;
const width = 7;
const area = calculateRectangleArea(height, width);
console.log("The area of the rectangle is "+area+" sq.cm.")